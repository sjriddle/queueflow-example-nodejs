# Where the QueueFlow server is listening.
QUEUEFLOW_URL=http://localhost:8000

# Bearer token. On the dev server any non-empty token authenticates.
QUEUEFLOW_TOKEN=dev

# Port for this Express service.
PORT=3000
